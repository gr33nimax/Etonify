# HydraBox 2.0 — task ledger

Машиночитаемый журнал исполнения `docs/HYDRABOX2_PLAN.md`. Единственный источник
ответа на вопрос «выполнен ли предшественник». Строку добавляет сам агент — последним
шагом задачи, после коммита. Поля `STATUS` в карточках плана истиной не являются.

Формат: `<TASK ID> | <repo> | <commit> | <RESULT>`

`RESULT`: `DONE`, `BLOCKED(<что ждёт>)`, `CANCELLED(<причина>)`,
`DEFERRED(<причина>)`.

| TASK ID | REPO | COMMIT | RESULT |
|---|---|---|---|
| H2-000 | hydrabox | 0297082c5a48a0be7674122fa9dc93dae1de0102 | DONE |
| H2-001 | HydraBox2 | HEAD | DONE |
| H2-002 | HydraBox2 | fffcdd5 | DONE |
| H2-003 | HydraBox2 | b5d10c7 | DONE |
| H2-001A | HydraBox2 | 0c0deaf | DONE |
| H2-100 | HydraBox2 | 41fe904 | DONE |
| H2-A01 | HydraBox2 | 27577a6 | DONE |
| H2-A02 | HydraBox2 | 91757c0 | DONE |
| H2-A03 | HydraBox2 | fffcdd5 | DONE |
| H2-A04 | HydraBox2 | 41fe904 | DONE |
| H2-A05 | HydraBox2 | b5d10c7 | DONE |
| H2-B01 | HydraBox2 | 321fa7b, 3f820bd | DONE |
| H2-B02 | HydraBox2 | 15eb0fc | DONE |
| H2-B03 | HydraBox2 | e74948a, f2bdaba | DONE |
| H2-B04 | HydraBox2 | 94ccdc2, 718af8d | DONE |
| H2-B04-ACC | HydraBox2 | — | DEFERRED(по разрешению владельца; device-приёмка перенесена в финальную валидацию) |
| H2-C01 | HydraBox2 | 1dd5716 | DONE |
| H2-C02 | HydraBox2 | 1dd5716 | DONE |
| H2-C03 | HydraBox2 | 206e896 | DONE |
| H2-C05 | HydraBox2 | d4fc25d | DONE |
| H2-C04 | HydraBox2 | 4fbb071 | DONE(device-приёмка отложена по разрешению владельца) |
| H2-D01 | HydraBox2 | 1edcaf5, 994a438 | DONE |
| H2-D02 | HydraBox2 | 1795f05, 7d5e0a7 | DONE |
| H2-D03 | HydraBox2 | 701fa1a, 5fde2c5 | DONE |
| H2-D04 | HydraBox2 | 0202524, bb6f9a5, a7e6191, d377ea0, 5c0c9ad, 355510e, 9ff4043, e4d5b48, 817151f, cc19d51, 549555a, 690642c | DONE |
| H2-D05 | HydraBox2 | b6d7a1b, fd41a73 | DONE |
| H2-D07 | HydraBox2 | 83eb60a, a003ff7 | DONE |
| H2-D06 | HydraBox2 | 2f0fbe7, 9546c18 | DEFERRED(M4 device-приёмка отложена по разрешению владельца; нет доступного устройства или AVD) |
| H2-A06 | HydraBox2 | ec3ac8f | DONE |
| H2-D08 | HydraBox2 | 616db59 | DONE |
| H2-D10 | HydraBox2 | 7910d47 | DONE |
| H2-D09 | HydraBox2 | 6a63a23 | DONE |
| H2-C06 | HydraBox2 | — | PARTIAL — снимок расширен полем traffic в проекции, но ядро счётчики ещё не публикует; см. §11 |
| H2-E01 | HydraBox2 | (alpha) | DONE — токены, тема, brand seed, две схемы моции, два уровня компонентов |
| H2-E02 | HydraBox2 | (alpha) | DONE — компоненты по инвентарю; аудит MD3 формально не прогонялся |
| H2-E03 | HydraBox2 | (alpha) | PARTIAL — навигация во всех трёх классах ширины (bottom bar / rail); матрица expressive на desktop не снята, desktop вне цели |
| H2-E04 | HydraBox2 | (alpha) | DONE — экран подключения и дашборд трафика поверх ScreenState |
| H2-E05 | HydraBox2 | (alpha) | PARTIAL — подписки ссылкой и список, выбор прокси; QR и импорт файла не реализованы |
| H2-E06 | HydraBox2 | (alpha) | PARTIAL — общие, DNS, inbound/MTU, диагностика, правовое согласие; бэкап и обновления только read-модели |
| H2-E07 | HydraBox2 | (alpha) | DONE — android-debug.apk, arm64-v8a, 2.0.0-alpha1 (versionCode 200) |
| ANDROID-ALPHA | HydraBox2 | (alpha) | DONE — цель владельца: доведён Android-фронт до собираемой alpha, ветка hb2 |
| H2-UX01 | HydraBox2 | 551bd85 | DONE — 25 findings по десяти осям + 9 функциональных разрывов; docs/HYDRABOX2_UX_REVIEW.md |
| H2-E08a | HydraBox2 | 551bd85 | DONE — реальная иконка и логотип из 1.x, edge-to-edge, TopAppBar, иконки навигации, три destination |
| H2-E10 | HydraBox2 | 551bd85 | DONE — composeResources, ru+en, правовые документы перенесены из app_ru.arb/app_en.arb |
| H2-E08b | HydraBox2 | 551bd85 | DONE — Connection вместо ScreenPhase, 35 кодов → 6 ситуаций, транспорт и коды только в диагностике |
| H2-E08c | HydraBox2 | 551bd85, 5cb11e8 | DONE — блокирующий первый запуск; баннер согласия с экранов убран |
| H2-E08d | HydraBox2 | 551bd85 | DONE — серверы и подписки объединены, переключение сервера без ручного разрыва, подтверждения опасных действий |
| H2-E08e | HydraBox2 | 551bd85, a75e7e9 | DONE — базовые и продвинутые настройки, следствия в подписях, предупреждение о переподключении |
| H2-D12 | HydraBox2 | a75e7e9 | DONE — blockLeaks, bypassLocalNetwork, режимы приложений, тема, язык через per-app locale Android 13+ |
| H2-D11 | HydraBox2 | a75e7e9 | PARTIAL — правила без rule-set: hijack по порту, icmp, STUN, локальная сеть, режимы приложений; adblock и РФ-маршруты ждут конвейера правил (G-02) |
| H2-E11 | HydraBox2 | 5cb11e8 | PARTIAL — импорт файлом, бэкап с парольной фразой, экран трафика; QR, цепочки и шаринг не реализованы (G-07) |
| H2-E12 | HydraBox2 | 5cb11e8 | PARTIAL — contentDescription, liveRegion, заголовки для ридера; контраст и цели нажатия на устройстве не измерены |
| H2-E09 | HydraBox2 | — | DEFERRED(тесты экранов снимаются после приёмки на устройстве; в окружении нет устройства и эмулятора) |
| H2-P1 | HydraBox2 | 85974e8, e40c8a0 | DONE — Clash и Xray через реальный путь импорта; типизированные отказы, метаданные подписки, HWID по политике 1.x; мёртвый parseDocument удалён |
| H2-P2 | HydraBox2 | bf9dfc9 | DONE — фрагментация TLS, strict route, реализация tun, TCP fast open и multipath, допуск url-test, разрыв соединений, уровень журнала доезжают до конфигурации |
| HAPP-LINKS | HydraBox2 | — | CANCELLED(владелец 2026-09-01: Happ crypto links не используются) |
| H2-P3 | HydraBox2 | aa8d08c | DONE — mixed-inbound, RuntimeMode.PROXY, старт без системного согласия, состояние «Прокси работает», порт и доступ из сети |
| H2-P4a | HydraBox2 | 74d4ef1 | DONE — core/ruleset: запись sing-box .srs (магия, версия 2, суккинктный трие) и разбор AdGuard-фильтра, побайтовая совместимость с 1.x |
| H2-P4b | HydraBox2 | (этот коммит) | DONE — загрузка списка AdGuard, компиляция на устройстве, атомарная замена файлов, метаданные и состояние набора |
| H2-P4c | HydraBox2 | (этот коммит) | DONE — rule_set в конфигурации, allow перед block, тумблер появляется только при скачанном наборе |
| H2-AUDIT1 | HydraBox2 | (этот коммит) | DONE — независимый аудит расхождений с 1.x; найден и закрыт отсутствующий `go.HydraNativeLoader` (ядро было недоступно из любого процесса), DNS-транспорт платформы, `endpoints` для WireGuard, разбор vmess/ss/транспортов, дедлайны рантайма, журнал и диагностика, платформенная интеграция; обе подписки владельца импортированы и туннель поднят на SM-S931B |
| H2-UI-FIX1 | HydraBox2 | (этот коммит) | DONE — вёрстка после редизайна: снят заголовок приложения (ActionBar платформенной темы + Compose TopAppBar), контент больше не уезжает под верхнюю панель, диск подключения масштабируется от размера окна и центрируется в реальном вьюпорте, единый левый край в списках, имена серверов в одну строку, отступы абзацев и инсеты первого запуска |
| H2-UI-FIX2 | HydraBox2 | (этот коммит) | DONE — описанные, но не выведенные настройки доведены до UI и конфигурации: ответы DNS (новая настройка `dns.strategy` вместо жёсткого `ipv4_only`), резолверы в туннеле и вне его с пресетами и своим адресом, содержимое уведомления (скорость/объём/оба вместо on/off), разрыв соединений при смене сервера, строгая маршрутизация, MTU, фрагментация TLS, реализация туннеля, экономия батареи, порт и LAN-доступ прокси; группы «Подключение/DNS/Маршрутизация/Приложения/Блокировка/Производительность/Совместимость/Интерфейс/Поддержка» без раздела «для продвинутых» |
| H2-UI-FIX3 | HydraBox2 | (этот коммит) | DONE — диагностика переделана: убраны состояние рантайма, транспорт, lanes и generations; остались версия, активный сервер, резолвер, последняя ошибка, уровень журнала и выгрузка. Журнал — отдельный экран: структурированные записи (время, уровень, источник, текст) из обоих процессов, LazyColumn, фильтр по уровню, монопространственный шрифт, свёртка повторов, раскрытие полной строки, очистка; описания в настройках сокращены или убраны |
| H2-UI-FIX4 | HydraBox2 | (этот коммит) | DONE — уведомление больше не зависает на «Отключаюсь…»: снимок с состоянием STOPPED снимает foreground-уведомление, а stop() делает то же после подтверждённого освобождения. Причина: интерфейсный процесс привязан к сервису, поэтому stopSelf() его не уничтожает, и Android не убирает уведомление живого сервиса |
| H2-UI-FIX5 | HydraBox2 | (этот коммит) | DONE — FakeIP: сервер `fakeip` с зарезервированным диапазоном 198.18.0.0/15, правило DNS только для запросов A/AAAA, `store_fakeip` в cache_file, настройка с персистом и строка в разделе DNS; выключено по умолчанию. Заодно исправлено, что выбранная стратегия DNS вообще не доезжала до TunnelInput |
| H2-UI-FIX6 | HydraBox2 | (этот коммит) | DONE — утечка DNS: `route.default_domain_resolver` был `dns-local`, то есть резолвер провайдера, и любое имя, которое аутбаунд набирает сам, спрашивалось у него (с FakeIP — каждое восстановленное маршрутизатором). Теперь `dns-direct`; у платформы остаётся только имя самого туннельного резолвера. Расширен выбор DNS (в туннеле — Cloudflare/Google/Quad9/AdGuard/Mullvad, вне — 1.1.1.1/8.8.8.8/9.9.9.9/94.140.14.14/77.88.8.8, плюс свой), диалог выбора прокручивается, формулировки FakeIP и строк DNS переписаны. Удалён мёртвый `ConfigGenerator` из альфы |
| H2-UI-FIX7 | HydraBox2 | (этот коммит) | DONE — bootstrap DNS как настройка (по умолчанию udp://77.88.8.8, есть «Резолвер сети» = device://network): через него `route.default_domain_resolver` и разрешение имён самих резолверов, поэтому в белых списках мобильного оператора туннель поднимается без правки настроек. Мёртвое поле `russiaDnsDirectResolver` заменено им с чтением старого ключа. Пресеты DNS разбиты по категориям DoH/DoT/UDP для всех трёх резолверов |
| H2-UI-FIX8 | HydraBox2 | (этот коммит) | DONE — контракт hydra-подписки: имя сервера берётся из `profiles[].name` (строка или карта переводов с `default`), а не из тега входа. «Обход БС» вместо `call-vk-out`. Тег остаётся идентичностью для конфигурации и выбора |
| H2-UI-FIX9 | HydraBox2 | (этот коммит) | DONE — закрыты выявленные риски: одинаковые теги в разных ресурсах hydra больше не теряют сервер (переименование в пределах ресурса с переписыванием detour и списков групп), профиль одного ресурса больше не делает выбираемым одноимённый сервер другого, уведомление не может вернуться поздним снимком после остановки (флаг stopping, в том числе для onRevoke), список приложений показывает «первые 200» и «выбрано N из 128» с недоступными переключателями на пределе вместо молчаливой потери выбора, `Libbox.setMemoryLimit` наконец применяется из настройки (1.x держит его включённым, у нас настройка существовала и не действовала) |
| H2-UI-FIX10 | HydraBox2 | (этот коммит) | DONE — расход батареи: замер `batterystats` показал 3 ч 4 мин CPU у процесса `:core` за 6 ч 29 мин работы (≈47% ядра непрерывно) при 3 м 45 с активного радио, то есть дело в CPU, а не в пробуждениях. Убраны множители на строку журнала ядра: уровень определяется по началу строки до любых аллокаций (было регулярное выражение и две строки на каждую отброшенную), `SetupOptions.debug` больше не привязан к типу сборки (это вызов через границу языка на каждую строку), добавлен уровень «Выключен» → `log.disabled` в конфигурации, единственное, что убирает форматирование строк на стороне ядра |
| H2-UI-DES1 | HydraBox2 | (этот коммит) | DONE — главный экран: полоса квоты подписки (использовано из лимита, «осталось N дн.»; при отсутствии лимита — только цифра без пустой шкалы), объём за сеанс рядом со временем в сети, спарклайн приёма и отдачи за последнюю минуту вместо двух статичных плиток, чипы «что включено» (Proxy, Реклама, FakeIP, «Вне туннеля: N») только когда это не значение по умолчанию, табличные цифры для всего, что обновляется раз в секунду. В проекцию добавлены числа: `usedBytes`/`totalBytes`/`quota`, `expiresInDays`, `updatedAt`, `uplinkRate`/`downlinkRate` |
| H2-UI-DES2 | HydraBox2 | (этот коммит) | DONE — подписки: список вместо формы со списком, добавление в modal sheet (ссылка не очищается до успеха), карточка источника с именем на всю ширину, бейджем шифрования, превью серверов по именам («Обход БС, amneziawg и ещё 2»), полосой квоты, датой обновления и действиями иконками в подвале, проблема — полосой внутри карточки рядом с действием, «Обновить все» при двух и более источниках. Иконка обновления перерисована: один arcTo больше половины круга читался как два отдельных штриха |
| H2-UI-DES3 | HydraBox2 | (этот коммит) | DONE — фирменная палитра: собственная схема M3 из зелёного семени (тёмная и светлая, все роли прописаны руками) вместо системной динамической, которая на Android 12+ бралась всегда и на оливковых обоях делала primary неотличимым от surface; выбор «Цвет: HydraBox / Системный» в разделе внешнего вида |
| H2-UI-DES4 | HydraBox2 | (этот коммит) | DONE — главный экран по образцу 1.x, а не Karing: плитка подписки сверху (имя, шкала только при наличии лимита, «использовано из», «осталось N дн.», обновление слева, переход в подписки), контрол по центру со словами состояния, подвал активного сервера снизу (значок, имя, протокол и задержка, три цифры ↓ ↑ ↔ при подключении, «Выберите сервер» до него). Чипы и график с главного убраны, график переехал на экран трафика, где 1.x и держит свои диаграммы. `ServerRef` получил тип протокола |
| H2-UI-DES5 | HydraBox2 | (этот коммит) | DONE — кнопка подключения по ТЗ: закрытый сундук с замком и обивкой при выключенном туннеле (только контур, без заливки), поднимающаяся крышка и растущие шеи при подключении, головы снаружи при подключённом (заливка primary плюс мягкое свечение), провисание голов при восстановлении вместо втягивания, дуга-развёртка вокруг диска только пока идёт работа и с учётом reduced motion |
| H2-UI-DES6 | HydraBox2 | (этот коммит) | DONE — внешний адрес: `ExitAddressProbe` через туннель (cdn-cgi/trace, две строки key=value, без JSON и ключей), флаг страны из regional indicators без ассетов, показ в подвале главного экрана с обновлением по тапу и очисткой при отключении. Это то, что 1.x показывал под именем сервера и чего у нас не было |
| H2-UI-DES7 | HydraBox2 | (этот коммит) | DONE — подписки: бейджи протоколов с количеством вместо имён через запятую, выключатель источника (серверы уходят из списка и из конфигурации, подписка остаётся), копирование ссылки, QR-код через zxing, обновление только расхода по тапу на шкалу (без замены документа), тап по карточке ведёт к серверам. На экране серверов — сортировка по источнику/задержке/имени и «перемерить» рядом с ней |
| H2-UI-DES8 | HydraBox2 | (этот коммит) | DONE — главный экран как приборная панель и новый герой вместо сундука (DES5 отменён владельцем: «по кнопке — хуйня»). Контрол — диафрагма: шесть лепестков, раскрытых при выключенном туннеле, повёрнутых и сомкнутых в цельный диск при подключённом; вокруг два кольца с числами — внешняя дуга расхода подписки и внутренний бег из шестнадцати штрихов, скорость которого равна измеренному трафику (тишина в канале — штрихи стоят). Слова состояния живут внутри диафрагмы. Под ней четыре строки-прибора: маршрут (протокол не дублирует имя сервера), внешний адрес, подписка и что вне туннеля. Значок обновления подписки крутится, пока запрос в полёте (был только серым). Срок и лимит выводятся всегда, в форме 1.x: «260.4 GiB / ∞» и «Осталось ∞ дн.» (на карточке подписки — «До <дата> · Осталось N дн.», «Истекла» после срока). Раньше строк не было вовсе: hydra-подписка владельца объявляет безлимит, а словесные «без лимита» и «бессрочно» владелец отклонил. Добавлен разбираемый, но выбрасываемый `updateIntervalHours` («Автообновление раз в 6 ч»). Четвёртая строка-прибор — «Режим подключения: VPN / Proxy» вместо «Защиты»: вторая строка появляется только когда есть что сказать — адрес прокси или «вне туннеля: N», — и строка `home_proxy_at` под диафрагмой убрана как дубль. Чип сортировки «по источнику» переименован в «как в подписке»: список и так сгруппирован по подписке, и чип не сортировал ничего. Кадры диафрагмы рендерятся off-device в `:ui:design:jvmTest` и сравниваются между состояниями |
| H2-PING1 | HydraBox2 | (этот коммит) | DONE — пинг перестал врать и ручной замер начал работать. Проекция читает не только цифру, но и вердикт ядра: `unavailable`/нулевая задержка — это `ProbeState.SILENT` и «не отвечает» в списке, а не пустая графа, неотличимая от неизмеренного сервера; задержка от секунды и выше выводится секундами («14.9 с» вместо «14885 мс» — это упёршийся в `C.TCPTimeout` замер, а не пинг). Ручной замер ушёл с `startURLTest` на `startURLTestWithOptions`: таймаут и конкурентность из настроек (до этого хранились и не действовали ни в конфиге, ни в API), приоритет — выбранному серверу, `force=true`, поэтому тап на «Проверить скорость» действительно перемеряет, а не отдаёт историю моложе интервала (30 мин). Дедлайн сессии — три таймаута, чтобы один молчащий сервер не забирал остальной список. Не тронуто и остаётся следующим шагом: пре-коннект замер (`StandaloneURLTestSession` в AAR есть) и ленивый подъём vk-моста — сейчас `Outbound.Start` поднимает 4 звонка × 4 воркера на каждый старт ядра независимо от выбора. |
| H2-TRANSPORT1 | HydraBox2 | (этот коммит) | DONE — здоровье транспорта читается у ядра, а vk-мост больше не поднимается зря. `TransportState` разбирает `libbox.hydraCoreTransportState()` (стадия, полосы, `applicable`, типизированный отказ с `domain`/`terminal`/`retry_after_ms`), контракт получил `totalLanes` и `retryAfterMillis` с расширением проводного формата; `CoreObserver` публикует снапшот при каждом изменении и пишет строку в журнал, но решать продуктовое состояние ему позволено только когда выбранный маршрут и есть vk-транспорт (`AppStore.selectedIsCallTransport`) — иначе падающий мост тащил бы рабочий VLESS в recovery; капча арминг 120-секундного дедлайна вместо отказа. Генератор конфигурации не включает `call`-аутбаунды, пока они не выбраны (`withoutIdleCallTransports`), с двумя оговорками: тег, на который ссылается `detour` или список группы, не выкидывается никогда, и если после выкидывания не осталось ни одного выбираемого сервера — не выкидывается ничего. Переключение на такой сервер перезапускает ядро (stop+start через deferredStart), потому что в живом конфиге его нет. ВАЖНО: аудит вечера был прочитан по устаревшему снапшоту `hydracore/client-debug46` (CHANGELOG debug.44); приложение несёт debug.51 = `d0de9624`. Там KCP-полос (`lane_tunnel.go`, `kcpSRTTMS`, 4×4) не существует — транспорт это QUIC поверх TURN-датаграмм, воркеров по умолчанию 4 (кратно `CallCount=4`, максимум 20), и RTT не измеряется нигде. Ping-часть аудита цела: `urltest.go`, `managed_urltest.go`, `turn_credentials.go`, `standalone_urltest.go` побайтово совпадают. |
| H2-CORE52 | HydraBox2 | (этот коммит) | DONE — ядро поднято до debug.52. В hydracore на `debug` лёг только релизный бамп `release/HYDRACORE_VERSION` (fa2a1ac2), поверх собственного `4965fb2b fix(vk-parasite): fail fast without active paths`, который в debug.51 не входил; ветка отправлена на GitHub, workflow `HydraCore - Verified Android and Linux runtime` (run 33689339095) собрал AAR, артефакт `hydracore-client-...` гидратирован в `platform/android/libs/libbox.aar`, provenance обновлён (версия, коммит, sha256 4b8e62fe…), сабмодуль `hydracore` переведён на тот же коммит. `verifyLibboxProvenance` и `verifyNativeLoaderSeam` проходят, APK поставлен, приложение на новом ядре открывает зашифрованную подписку через `hydra-gate` и разбирает 4 из 4 серверов. НЕ входит в .52: патч RTT — в QUIC-дизайне round trip не измеряется нигде, нужен `logging.ConnectionTracer` на путь (`quicConfig`/`dialQUIC` в `quic_conn.go`), аксессор на `QUICRelay` (`quicPathConn` в `quic_relay.go`) и поле в `Client.healthSnapshot` (`supervisor.go`); непроверяемую правку транспорта в релизную сборку не кладу. |
| H2-AUDIT2 | HydraBox2 | (аудит, без правок кода) | DONE — клиентский аудит на устройстве SM-S931B (Android 16, debug-сборка 2.0.0-alpha1, ядро debug.52). Прогоны и артефакты в `D:\dev\hb2-test\logs`. **Измерено.** Один телефон, один Wi-Fi, WARN, экран выключен, один генератор нагрузки, один и тот же VPS на выходе (31.77.203.66/FI): `call-vk-out` — 24.84% одного ядра, 180.4 МБ, 732 КБ/с, 0.339 ядро·с/МБ, оверхед на проводе 14.3%; `vless-xhttp` — 12.28%, 316.2 МБ, 1278 КБ/с, 0.096 ядро·с/МБ, оверхед 10.8%. Простой (туннель поднят, трафика нет): VK 1.42% ядра, VLESS 1.21% — вся VK-специфичная надбавка простоя 0.21 п.п., включая keepalive восьми полос. Уровень журнала: DEBUG 266 строк/с против WARN 3 строк/с — CPU `:core` не изменился (27.1% → 28.2%, шум), то есть журнал не объясняет стоимость транспорта; цена лога лежит в `logd` (0.30–0.57% ядра, чужой uid), в 82% системного буфера logcat (63 388 строк HydraBox из 77 482 за 4 минуты) и в перезаписи всего 24 КБ blob журнала каждые 1.2 с в rollback-journal (WAL выключен). Хендовер Wi-Fi↔LTE **работает**: `generation 6`, трафик целиком ушёл в wlan0, `rmnet_data8` не сдвинулся; повторяющиеся `generation 1` в старом журнале — разные жизни процесса `:core`, а не сломанный счётчик. `ciCheck` зелёный: 402 юнит-теста, 0 падений, lint 14 warnings / 0 errors; инструментальных тестов нет ни одного, `platform/android` (4260 строк) не покрыт. **Опровергнуто измерением:** гипотеза «debug-логи съедают CPU»; гипотеза «конверт RTP/DTLS/TURN раздувает аплинк» (14.3% против 10.8% — не амплификация; бакет netstats 16.7 МБ вверх / 3.1 МБ вниз был реальной отдачей). **Code audit против qWDTT (`references/TURNVK/wdtt/proxy-turn-vk-android`).** По сопоставимым механизмам горячего и холостого пути явного performance/energy regression или ошибки портирования в `vk_parasite` не найдено. Это и есть ответ на исходный вопрос, и code audit для него достаточен: qWDTT здесь эталон архитектуры и реализации, а не объект лабораторного сравнения. Сравнимые участки находятся как минимум на уровне референса, часть реализована эффективнее: AEAD хранится в `rtpCodec` (референс ищет через `sync.Map` с аллокацией `string(key)` на пакет), буферы через `sync.Pool` (референс — `make` на пакет плюс `make` на nonce), паддинг из `randv2.ChaCha8` (референс — `crypto/rand.Read` дважды на пакет), бюджет MTU посчитан и закрыт `TestWrappedPacketFitsPathMTU`, TCP-поток прибит к одному QUIC-стриму на одном path, поэтому не нужен `chunkSizeFor`/`maxDwellMS`/`PrioCh` референса против reorder между relay, UDP идёт QUIC DATAGRAM со своей фрагментацией. Единственная проводная разница — +8 Б расширения RFC 8285 в RTP-заголовке, величина незначимая. **Цена архитектуры — вопрос, отдельный от аудита реализации.** Измеренная разница CPU против VLESS-XHTTP отвечает не на «есть ли ошибка портирования относительно qWDTT», а на «какова цена выбранной архитектуры относительно обычного транспорта». Root cause этой разницы не локализован: объяснение через quic-go batching (отсутствие GSO/GRO на `datagramPacketConn`, ACK/pacing, глубина стека QUIC→DTLS→obfs→TURN) остаётся гипотезой, для утверждения нужен CPU-профиль с атрибуцией по функциям. Количественное утверждение «`vk_parasite` дороже/дешевле qWDTT на X%» не делается и для задачи не требуется. **Решения владельца (не переоткрывать):** 8 lanes оставить, это осознанная архитектура (path diversity, failover, устойчивость к плохим relay; референс рассчитан на 9/18/27+ workers, и один worker qWDTT ≠ один lane Hydra — worker референса это packet-relay очередь, lane Hydra несёт полный QUIC-стейт); keepalive 15 с оставить, отношение 15/60 не основание, а `15с / 8 lanes = 1.875с` не считается арифметически, потому что таймеры коалесцируются и состояние радио определяется реальным packet timing/RRC; QUIC не заменять; RTP/obfs, MTU и фрагментацию не переделывать. Порядок работ: сначала подтверждённые дефекты жизненного цикла (см. H2-FIX-A…J), профилирование транспорта — только после них и только при необходимости. **Побочный эффект аудита:** `singbox_log_level` на устройстве переведён с `debug` на `warn` (значение `debug` было оставлено от прежней отладки; default модели — `WARN`) |
